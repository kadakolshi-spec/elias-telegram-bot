# Legacy reference only. New Railway services ignore Config as Code.
# Configure one replica and On Failure / 3 retries in the dashboard.
[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "python bot.py"
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
